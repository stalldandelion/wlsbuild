.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install wlsbuild

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wlsbuild
    $ pip install wlsbuild
